function divisão(dividendo, divisor) {
    let resultado = dividendo / divisor
    let resto = dividendo % divisor
    return console.log(`O resultado da divisão é igaul a ${resultado}, sobrando ${resto}.`)
}

divisão(10, 3)